# The Service principal is created in AD
# Managed Identity is only created on the resource
# Assign role to managed identity(resiource)

# AKS Cluster has many name-spaces and pods are inside name-spaces